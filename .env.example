# Variáveis de ambiente para Spring Boot
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# Banco de dados (exemplos)
DB_URL=jdbc:postgresql://localhost:5432/app
DB_USERNAME=app
DB_PASSWORD=app

# Segurança / JWT (exemplo)
JWT_SECRET=troque-esta-chave
